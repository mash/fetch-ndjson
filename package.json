{
  "name": "fetch-ndjson",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build-ts && npm run lint",
    "build-ts": "tsc",
    "test": "ts-node node_modules/tape/bin/tape test/**/*.ts",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "npx --no-install tslint --fix --project tsconfig.json",
    "test-server": "PORT=8080 nodemon test/server.js",
    "test-build": "cd test && webpack -w"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/tape": "^4.13.0",
    "nodemon": "^2.0.4",
    "prettier": "^2.0.5",
    "tape": "^4.13.0",
    "ts-loader": "^7.0.5",
    "ts-node": "^8.10.2",
    "tslint": "^6.1.2",
    "tslint-config-prettier": "^1.18.0",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  },
  "dependencies": {}
}
